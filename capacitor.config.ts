import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'task-app-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
